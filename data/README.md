# Data directory

Production raw data is intentionally not committed to Git.

The future pipeline should use content-addressed object storage for immutable source content and retain only manifests/checksums in the repository or database.
