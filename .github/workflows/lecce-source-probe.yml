name: Lecce source boundary probe

on:
  push:
    branches:
      - expansion/lecce-2026-09-18
    paths:
      - scripts/lecce_semantic_probe2.py
      - .github/workflows/lecce-source-probe.yml

permissions:
  contents: write

jobs:
  probe:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
        with:
          ref: expansion/lecce-2026-09-18
      - name: Install PDF tooling
        run: python -m pip install --disable-pip-version-check pdfplumber
      - name: Audit official Lecce sources
        run: python scripts/lecce_semantic_probe2.py
      - name: Upload transient source captures
        uses: actions/upload-artifact@v4
        with:
          name: lecce-source-captures
          path: |
            tmp/lecce-semantic2/lecce_listed.pdf
            tmp/lecce-semantic2/lecce_applicants.pdf
          retention-days: 1
      - name: Persist compact semantic receipt
        run: |
          mkdir -p tmp
          cp tmp/lecce-semantic2/summary.json tmp/lecce_semantic_summary.json
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add tmp/lecce_semantic_summary.json
          if git diff --cached --quiet; then
            echo "Semantic receipt unchanged"
            exit 0
          fi
          git commit -m "chore: capture Lecce semantic source receipt"
          git push origin HEAD:expansion/lecce-2026-09-18
