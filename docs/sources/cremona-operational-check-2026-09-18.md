# Cremona White List operational check — 18 September 2026

The Prefettura di Cremona official **White list - Elenco Ditte** page was reverified on 18 September 2026 and exposes the current attachment `white-list-elenchi-ditte_3.pdf`. The landing-page metadata still displays 10 August 2026, while the live attachment is stored under the September 2026 publication path and contains source rows dated through September. Because the document itself does not state a single edition date, the public configuration uses **18 September 2026 as the verified capture/reference date** and does not infer a publication date from the stale landing metadata.

Repeated cache-bypassed GETs of the current official PDF were byte-identical: **438,436 bytes**, SHA-256 `55a9498a76120f0b781f9c7ae8da7144c141551598b63a98b0bc52caac79a550`. The six-page PDF contains one structured table per page; fail-closed extraction yields page denominators **32, 38, 36, 39, 37, 5**, exactly **187 source observations** numbered 1–187.

The single official table positively contains both logical populations. It yields **99 `listed`**, **50 `renewal_update_in_progress`**, and **38 `pending`** observations. The pending population consists of 37 rows explicitly marked `ISCRITTO ELENCO RICHIEDENTI - IN ISTRUTTORIA` plus one row (`TRE.SO`, source ordinal 180) explicitly marked `FATTA ISTANZA IN DATA 18/06/2025` with no listing/expiry pair. Renewal/update status is taken only from the explicit `FATTA ISTANZA A PERMANERE` source wording, including the reviewed source typo `PERAMERE`; row 128 retains its historical listing/expiry fields alongside the explicit renewal note without inferring any additional legal effect.

Structured identifier coverage is **183/187**. Four malformed numeric source tokens remain raw-only and are never padded, truncated or repaired: row 11 `009226901930`, row 19 `1168200192`, row 63 `017747090193`, and row 90 `0136901033`. Eighteen observations explicitly contain both a 16-character codice fiscale and an 11-digit VAT identifier; their source-cell line boundary is preserved for parsing. Legal-seat locality and ten section markers are retained as published, without address enrichment or inferred activity membership.

Publication is byte-pinned (`raw_sha256`) and fail-closed on page count, per-page row counts, row numbering, table width/header structure, section-marker vocabulary, status distribution, identifier classes and structured-identifier coverage. The combined series therefore satisfies both listed and applicant source-population requirements by positive row-level evidence rather than by absence or failed search.
